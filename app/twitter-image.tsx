// Twitter card image — re-uses the OpenGraph image generator so we have one
// source of truth for the share preview. Twitter prefers the same 1200×630
// canvas with summary_large_image.

export { default, alt, size, contentType } from "./opengraph-image";
